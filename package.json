{
  "name": "jacquard-js-runtime",
  "version": "0.1.0",
  "description": "a JS runtime for the Jacquard bytecode format",
  "main": "dist/index.js",
  "repository": "https://github.com/StirfireStudios/Jacquard-js-Runtime",
  "author": "Stirfire Studios",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0"
  },
  "scripts": {
    "build": "babel src -d dist",
    "prepare": "yarn build",
    "serveDoc": "documentation serve --watch src/** -f html",
    "buildDoc": "documentation build src/** -f html -o docs"
  },
  "dependencies": {
    "utf-8": "^2.0.0"
  }
}
